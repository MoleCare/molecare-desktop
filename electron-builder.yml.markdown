# Electron Builder Build Matrix
| Platform    | Result        |
|-------------|---------------|
| macOS-latest | Pass          |
| windows-latest | Fail          |
| ubuntu-latest  | Fail          |

# Notes:
- Run `npm install` and then `npm run pack` or build for Windows/Linux respectively.
- Test window opening, tray functionality, and packaging output.